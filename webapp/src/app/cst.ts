export namespace Cst {
    export enum table_style_class {
        p_datatable_sm = "p-datatable-sm",
    }

    export enum msg {
        sev_success = "success",
        sev_info = "info",
        sev_warn = "warn",
        sev_error = "error",
    }

    export enum tag {
        sev_success = "success",
        sev_info = "info",
        sev_warning = "warning",
        sev_danger = "danger",
    }

    export enum button {
        // severity
        sev_secondary = "p-button-secondary",
        sev_success = "p-button-success",
        sev_info = "p-button-info",
        sev_warning = "p-button-warning",
        sev_help = "p-button-help",
        sev_danger = "p-button-danger",
        // size
        size_small = "p-button-sm",
        size_large = "p-button-lg",
        // other
        link = "p-button-link",
        text = "p-button-text",
        outlined = "p-button-outlined",
        raised = "p-button-raised",
        rounded = "p-button-rounded",
    }
}
